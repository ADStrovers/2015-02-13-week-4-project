<div>
  <label for="<%= req %>"><%= req.capitalize.delete("_id") %></label>
  <select>
    <% @convention_list.each do |item| %>
      <option value="<%= item.id %>"><%= item.name %></option>
    <% end %>
  </select>
</div>